description: Not found error
content:
  application/json:
    schema:
      type: object
      required:
        - message
      properties:
        message:
          type: string
          example: Failed to send the email, please try again later.
